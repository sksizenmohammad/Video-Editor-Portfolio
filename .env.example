# Admin password for /admin (set in Vercel → Settings → Environment Variables)
ADMIN_PASSWORD=your-secure-password-here

# Client portal access code — share this + register client Gmail in data/clients.json
CLIENT_ACCESS_CODE=smilo2026

# Optional: separate secret for client sessions (defaults to ADMIN_PASSWORD)
# SESSION_SECRET=your-random-secret-string
